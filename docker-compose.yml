services:
  api:
    build:
      context: .
      target: dev
    container_name: mydream-api
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    stdin_open: true
    tty: true
